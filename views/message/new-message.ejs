<% if (title) {%>
    <div id="new_message">
      <h1>
        <%= title %>
      </h1>
      <%- messages() %>
  
        <!-- Error message block -->
        <% if (errors) { %>
          <ul class="notice">
            <% errors.array().forEach(error=> { %>
              <li>
                <%= error.msg %>
              </li>
              <% }) %>
          </ul>
          <% } %>

          <form action="/message/new_message" method="post">
            <input type="number" name="new_message_sender" id="new_message_sender" value="<%= locals.accountData.account_id %>" hidden>

            <label for="new_message_recipient"><strong>To:</strong></label>
            <%- recipientList %>

            <label for="new_message_subject"><strong>Subject:</strong></label>
            <input type="text" name="new_message_subject" id="new_message_subject" value="<%= locals.new_message_subject %>" required>

            
            <label for="new_message_content"><strong>Message:</strong></label>
            <textarea name="new_message_content" id="new_message_content" cols="30" rows="10" value="<%= locals.new_message_content %>" required></textarea>

            <button type="submit">Send</button>
          </form>
          
<% } else { %>
    <% res.redirect('/') %>
<% } %>

